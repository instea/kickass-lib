# Kickass library analyser

Do you want to find out if a library is kickass or it will just kick your ass?

## Development
Developer stack: `yarn` for dependencies, `eslint`, `prettier` for formatting, `flow`
